# vim: filetype=hyprlang
################
### MONITORS ###
################

monitor=,1920x1080@165.00Hz,0x0,1

####################
### NVIDIA STUFF ###
####################

# env = LIBVA_DRIVER_NAME,nvidia
# env = XDG_SESSION_TYPE,wayland
# env = GBM_BACKEND,nvidia-drm
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia

# env = NVD_BACKEND,direct

cursor {
    # NOTE:
    # hardware cursor without allow_dumb_copy used to work
    # before Hyprland commit e6cf643f5ab1c1545fb858ab1fd9d7538ef9e0f3 if i'm not mistaken.

    # no_hardware_cursors = true # uncomment this if below has a problem.
    allow_dumb_copy = true # uncomment this if you want hardware cursor.
}

#############
### INPUT ###
#############

input {
    accel_profile = flat
}
